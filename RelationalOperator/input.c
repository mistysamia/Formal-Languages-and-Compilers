#include <string.h>
int main(void) {

  int a, b;
  if (a + b <= 5)
    printf("Hi"); /*'+' Art-op
++
&&
    erferf
 */
  a = 5;
  else if ((b * 2) < 5 && (a / 2) == 0)
      // i ||d
      printf("/ is art-op.");
}
